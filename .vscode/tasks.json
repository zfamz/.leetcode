{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run node",
      "type": "process",
      "command": "node",
      "args": ["--no-warnings", "--watch", "${relativeFile}"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "test",
        "isDefault": true
      }
    }
  ]
}
